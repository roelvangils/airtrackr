__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv/

# Screenshots
screenshots/

# Database — the WHOLE directory, no exceptions. Individual patterns kept missing
# sidecars: a rollback journal made it into the public history in 2025 carrying real
# location pages, and a WAL/SHM pair nearly repeated that in 2026. Nothing under
# database/ ever belongs in git.
database/

# Logs
logs/

# Secrets & config with sensitive data
.api_key
.env
config.json
start_tunnels.sh
dashboard/.env

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo
# Dashboard build output — regenerate with `cd dashboard && bun run build`.
# Never commit it: Vite inlines VITE_API_KEY into the bundle.
dashboard/dist/

# Debug output from the old screenshot/OCR pipeline
temp_regions/
dashboard/node_modules/
